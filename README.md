## Setup

- Install Node.js 18+.
- Copy `.env.example` to `.env` and fill values:
  - `RPC_URL` (Base RPC)
  - `PRICE_ORACLE_ID` (e.g., 58)
  - `PRIVATE_KEYS` (comma-separated)

```
cp .env.example .env
npm install
npm start
```